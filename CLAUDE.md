# EGOMOT — Claude Code иштөө эрежелери

## Долбоор жөнүндө

EGOMOT — Кыргызстандагы электр трицикл тетиктерин соодалоо бизнеси үчүн башкаруу системасы: Кытайдан сатып алуу → логистика → FIFO склад → сатуу → касса → отчеттор.

## Source of Truth — ЭҢ МААНИЛҮҮ ЭРЕЖЕ

Бардык бизнес-логиканын бирден-бир булагы — **`docs/EGOMOT_Knowledge_Base_v2.1.md`**.

- Ар бир модулду жазуудан МУРДА билим базанын тиешелүү §-бөлүмдөрүн оку.
- Билим базага каршы келген код ЖАЗБА. Карама-каршылык же түшүнүксүз жер тапсаң — код жазбай, адегенде суроо бер.
- Билим базада жок жаңы бизнес-эреже керек болсо — өзүң ойлоп таппа, суроо бер.
- Ар бир PR/коммитте кайсы § ишке ашырылганын жаз (мис. `feat(sales): FIFO COGS block §13.3–13.4`).

## Стек

- **Database:** PostgreSQL 15+. Schema: `db/egomot_schema.sql` — булактан өзгөртүлөт, миграциялар менен жүрөт.
- **Backend:** TypeScript + NestJS + Prisma. Бир монолит, микросервис ЖОК.
- **Frontend:** React + Vite, PWA, mobile-first (билим база §1). UI тили — кыргызча/орусча аралаш, код/идентификаторлор англисче.
- **Тесттер:** Jest/Vitest. Финансылык логика тестсиз кабыл алынбайт.

## Бузулгус инварианттар (билим базадан)

Кайсы модулда иштебе, ушулар ар дайым сакталат:

1. **Ар бир акча/склад кыймылы документке байланышат** (§27, §42.3–4). Балансты түз UPDATE кылган код жазылбайт — баары document → movement аркылуу.
2. **CONFIRMED документ өзгөрбөйт** (§27.1). Оңдоо = RET же COR. Update endpoint'тер CONFIRMED статуста 409 кайтарат.
3. **FIFO**: сатууда эң эски `layer_date` биринчи; возврат = баштапкы cost + бүгүнкү дата менен ЖАҢЫ layer (§18, §18.0). Орточолоштуруу ЖОК.
4. **Терс калдык болбойт**: layer_stock.qty >= 0, валюталык касса >= 0 (§42.5–6). Транзакция ичинде текшерилет.
5. **Allocation тыйынга чейин так**: Σ allocated = source amount, remainder deterministic rule менен эң чоң позицияга (§9.9).
6. **Final Price >= FIFO COGS** кадимки Sale'да — эч ким override кылбайт, OWNER да (§13.4). Өздүк нарктан төмөн = LSS процесси гана.
7. **Payment Allocation**: демейки FIFO (эң эски карыз), ар бир PAY → sale allocation lines (§16-А). Бонус PAYABLE ушуга таянат (§23.2).
8. **Period Lock**: OPEN → CASH_HANDED → DAY_CLOSED → MONTH_CLOSED. Жабылган датага жаңы документ түзүлбөйт; Day Close pre-check: unresolved docs = 0 ЖАНА бардык кассирлер CASH_HANDED.
9. **Документ номери**: PREFIX-YYYY-NNNNNN, DB транзакция ичинде SELECT FOR UPDATE менен берилет, эч качан кайра колдонулбайт.
10. **Акча суммалары**: NUMERIC/Decimal гана. `number`/float менен акча эсептөө — катуу тыюу. Тегеректөө: 0.01 KGS, banker's rounding эмес, кадимки half-up.

## Коопсуздук

- PIN plaintext эч жерде сакталбайт/логдолбойт — bcrypt/argon2 hash гана.
- PIN талап кылынган операциялар: Return, Write-off, Advance Refund, Day Close, COR, Inventory Adjustment — ар дайым; Sale — шарттуу (скидка / карыз / порогдон жогору).
- Audit Log жана Security Log — өзүнчө таблицалар, эч качан UPDATE/DELETE кылынбайт (append-only).
- Бардык mutating endpoint'тер idempotency key кабыл алат (Online-only бөлүмү, duplicate коргоо).

## Иштөө тартиби

1. Модулдарды **Приоритет 1 тартибинде** кур (§41): Пайдубал → Капитал/CEX → PUR/SPY → RCV/LOT/Allocation → Склад → Кардар → SAL → PAY.
2. Ар бир модул = өзүнчө тапшырма. Бир тапшырмада бир нече модулду аралаштырба.
3. Ар бир модулдун аягында: unit тесттер (өзгөчө акча/FIFO/тегеректөө), билим базадагы мисалдарды түз тест кылып жаз (мис. §9.9: 1000.00 → 333.34+333.33+333.33; §18: 12 мотор FIFO мисалы; §16.3 кредит лимит мисалы).
4. Миграциялар кайтарылгыс болбосун — ар бирине down script.
5. Датасы/убактысы: сервер UTC сактайт, business logic Asia/Bishkek менен иштейт.

## Код стили

- Түшүнүктүү, жөнөкөй код > "акылдуу" код. Команда кичине, кызматкерлер алмашат.
- Service катмарында бизнес-логика; controller жука; SQL/Prisma репозиторийде.
- Ар бир финансылык service методу транзакция ичинде иштейт (Prisma $transaction / SERIALIZABLE керек жерде).
- Комментарийлерде билим базанын §-номерин шилте.
